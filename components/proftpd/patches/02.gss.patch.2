Index: mod_gss-1.3.3/configure
===================================================================
--- mod_gss-1.3.3/configure
+++ mod_gss-1.3.3/configure
@@ -4304,7 +4304,7 @@ if test "$enable_arg" = "no"; then
 		    check_mit
                   fi
                   ;;
-           SunOS) pkginfo SUNWgss 2>&1 >/dev/null
+           SunOS) ls -l /usr/lib/libgss.so.1 2>&1 >/dev/null
                   if test $? == 0 ; then
                     check_seam
 		  else
Index: mod_gss-1.3.3/configure.in
===================================================================
--- mod_gss-1.3.3/configure.in
+++ mod_gss-1.3.3/configure.in
@@ -271,7 +271,7 @@ if test "$enable_arg" = "no"; then
 		    check_mit
                   fi
                   ;;
-           SunOS) pkginfo SUNWgss 2>&1 >/dev/null
+           SunOS) ls /usr/lib/libgss.so.1 2>&1 >/dev/null
                   if test $? == 0 ; then
                     check_seam
 		  else 
